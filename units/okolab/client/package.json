{
  "scripts": {
    "build": "npm run build:js && (npm test || true)",
    "build:js": "esbuild src/index.ts --bundle --external:*.css --external:pr1 --external:react --format=esm --minify --outdir=../src/pr1_okolab/client",
    "test": "tsc"
  },
  "devDependencies": {
    "@types/react": "^18.0.16",
    "esbuild": "^0.14.53",
    "pr1": "file:../../../client",
    "sass": "^1.54.4",
    "typescript": "^4.8.2"
  }
}
