on: push
jobs:
  # build-ubuntu:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: '16'
  #     - run: npm install --prefix client
  #     - run: npm run --prefix client build
  #     - run: ./app/server/build.sh
  #     - run: npm install --prefix app/electron
  #     - run: npm run --prefix app/electron build
  #     - uses: actions/upload-artifact@v3
  #       with:
  #         name: build-ubuntu
  #         path: app/electron/out/make/zip/**/*.zip
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Build the client
        run: |
          npm install
          npm run build
        working-directory: client
      - name: Build the server
        run: |
          python -m venv env
          ./env/Scripts/activate
          python setup.py install
          pip install ../../host
          pyinstaller --noconfirm main.spec
          deactivate
        working-directory: app/server
      - name: Build and package the app
        run: |
          npm install
          npm run build
        working-directory: app/electron
      - run: ls app/electron/out/make
      - run: ls app/electron/out/make/squirrel.windows
      - uses: actions/upload-artifact@v3
        with:
          name: build-windows
          path: app/electron/out/make/squirrel.windows/*
